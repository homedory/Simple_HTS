<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Balance</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 50px;
        }
        .balance {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .buttons {
            display: flex;
            gap: 15px;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
        }
        .home-button {
            position: absolute;
            top: 20px;
            left: 20px;
            padding: 10px 15px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            cursor: pointer;
        }
        .home-button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <a href="/" class="home-button">홈</a>

    <% if (typeof requestResponse !== 'undefined' && requestResponse === 'fail') { %>
        <script>alert("<%= message %>");</script>
    <% } %>

    <div class="balance">
        예수금: <%= balance %>원
    </div>

    <div class="withdrawal">
        <h2>인출</h2>
        <form action="/myinfo/withdrawal" method="POST">
            <input type="number" name="amount" placeholder="인출 금액을 입력하세요" 
                   min="0" required>
            <button type="submit">인출</button>
        </form>
    </div>

    <div class="deposit">
        <h2>입금</h2>
        <form action="/myinfo/deposit" method="POST">
            <input type="number" name="amount" placeholder="입금 금액을 입력하세요" 
                   min="0" required>
            <button type="submit">입금</button>
        </form>
    </div>

    <div class="buttons">
        <button onclick="location.href='/myinfo/mystock'">보유 주식</button>
        <button onclick="location.href='/myinfo/history'">거래 내역</button>
    </div>
</body>
</html>
